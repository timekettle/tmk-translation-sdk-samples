source 'https://cdn.cocoapods.org/'
source 'https://github.com/timekettle/TmkTranslationSDK-iOS.git'

platform :ios, '15.0'

target 'SDKDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SDKDemo
  # 预发布(beta 渠道)版本的 podspec 只提交到 spec 仓库的 beta 分支,未推送到 trunk/CDN,
  # 故通过该渠道分支上 podspec 的 raw URL 直接引用。
  pod 'TmkTranslationSDK', :podspec => 'https://raw.githubusercontent.com/timekettle/TmkTranslationSDK-iOS/beta/Specs/TmkTranslationSDK/1.2.0-beta16/TmkTranslationSDK.podspec'
  pod 'SnapKit'

  target 'SDKDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SDKDemoUITests' do
    # Pods for testing
  end

end
